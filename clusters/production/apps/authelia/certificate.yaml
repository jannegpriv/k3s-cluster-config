apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: auth-k3s-nu
  namespace: authelia
spec:
  secretName: auth-k3s-nu-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
    - auth.k3s.nu
