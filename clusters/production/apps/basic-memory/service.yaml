apiVersion: v1
kind: Service
metadata:
  name: basic-memory
  namespace: basic-memory
  labels:
    app.kubernetes.io/name: basic-memory
    app.kubernetes.io/instance: basic-memory
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: basic-memory
    app.kubernetes.io/instance: basic-memory
  ports:
  - name: mcp
    port: 8000
    targetPort: mcp
