# Popup page opened from cameras_landet (carport block). Only while this page is open does the
# browser request the HLS playlist -> openHAB starts ffmpeg -> go2rtc opens the
# Tapo session to the C425. Close the popup and the chain winds down
# (binding stops HLS ~64 s after the last playlist request).
uid: cameras_carport_c425_live
tags:
  - cameras
props:
  parameters: []
  parameterGroups: []
timestamp: Sep 12, 2026, 1:30:00 PM
component: oh-layout-page
config:
  label: Carport – C425 (live)
  sidebar: false
slots:
  default:
    - component: oh-block
      slots:
        default:
          - component: oh-grid-row
            slots:
              default:
                - component: oh-grid-col
                  slots:
                    default:
                      - component: oh-video-card
                        config:
                          title: Carport – C425
                          footer: Stäng popupen när du är klar – kameran går tillbaka till vila.
                          url: /ipcamera/c425_carport/ipcamera.m3u8
                          type: application/x-mpegURL
                          startManually: true
                          startMuted: true
                          hideControls: false
                          playerType: videojs
                          posterURL: /static/camera-idle.jpg
